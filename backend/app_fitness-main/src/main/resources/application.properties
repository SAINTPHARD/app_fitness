spring.application.name=appfitness

# Local development is the default; Railway explicitly sets SPRING_PROFILES_ACTIVE=prod.
spring.profiles.default=dev

# Optional local .env imports. Production uses platform environment variables.
spring.config.import=optional:file:.env[.properties],optional:file:../../.env[.properties]
